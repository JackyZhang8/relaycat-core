[package]
name = "relaycat-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Shared MessagePack protocol definitions and frame logic for the RelayCat CLI and relay server."
repository = "https://example.invalid/relaycat-protocol"

[workspace]

[dependencies]
flate2 = "1.0"
rmp-serde = "1.3"
serde = { version = "1.0", features = ["derive"] }
